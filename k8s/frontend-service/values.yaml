replicaCount: 1

image:
  repository: solid-frontend
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: traefik-solid
  host: localhost
  tls:
    enabled: false
    secretName: ""
