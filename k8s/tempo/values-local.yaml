# Local Tempo (grafana/tempo, single-binary) — chart defaults are sufficient.
# Present so the release is discoverable from the tree:
#   helm upgrade --install tempo grafana/tempo -f k8s/tempo/values-local.yaml
